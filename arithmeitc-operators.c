#include <stdio.h>

int main ()
{
    // arithmetic operators

    // + (dodawanie)
    // - (odejmowanie)
    // * (mnożenie)
    // / (dzielenie)
    // % (modulus)
    // ++ (increment)
    // -- (decrement)

    int x = 5;
    int y = 2;
    
    y++;
    
    printf("%d ", y);

    return 0;
}